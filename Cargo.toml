[package]
name = "effigy"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Unified task runner with nested catalog resolution"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
