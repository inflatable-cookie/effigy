# g05 — Secret and Reusable-Core Hardening

Status: archived
Kind: roll-up
Succeeded by: `g06`

## Generation intent and boundary

`g05` established safe local secret/configuration management and used the
post-release window to reduce reusable-core ownership debt.

## Shipped outcomes

- Secret/local-config grammar, built-in encrypted vault, redaction, task,
  container, Rhai, and deploy injection.
- Gateway route-table trust hardening and post-release reference cleanup.
- Reusable-core extraction, schema-shape consolidation, process execution
  boundaries, and low-risk deduplication.

## Current canonical destinations

- Secret and Varlock posture: [`contracts/032`](../../contracts/032-secret-and-local-config-management-contract.md).
- Gateway trust: [`contracts/033`](../../contracts/033-gateway-route-table-trust-contract.md).
- Current crate ownership: [`architecture/010`](../../architecture/010-package-map.md).

## Material migrations, limits, and retained risks

- The native encrypted vault is the supported local-secret path.
- Varlock remains an explicitly deferred adapter, not an operational backend.
- `v0.7.0` is the material release checkpoint.

## Deferred or unresolved items

- Any Varlock implementation requires fresh planning under contract `032`; no
  executable g05 item survives.

## Selected evidence

- Frozen source at switchover commit `7412d9ab182cf1070ea00e280df9aa2a21dafb6c`,
  tree `bbd7aa9ece3d5088b78c5a72acf0d2890a2daec6`, 79 Markdown files.
- Full milestone and batch-card evidence remains in Git and archived logs.

## Succeeding generation

`g06` ran the bounded codebase lean-down and ownership simplification suite.
