# Demo Post-Concurrent-Runtime-Projection-Shape Boundary Decision

Date: 2026-04-12
Roadmap: `g02.003`
Batch: `075`

## Summary

The next slot does not go back to browser chrome. It stays runner-side for one
more bounded concurrent-runtime truth slice: projected-process summary facts
for demos that stay on the flattened multi-process path.

## Decision

- do not widen into multi-process browser controls next
- do not pause this branch yet
- next slice is bounded runner-owned projected-process summary truth for
  concurrent-runner demos that stay projected

## Why

- `projection_shape` solved whether one live terminal is honest
- projected demos still hide too much about what is behind that projection
- browser work now would either guess or stay vague about the underlying
  managed processes
- one more bounded runner contract slice keeps later browser follow-up honest
  without turning the demo browser into a process manager

## Vision Target Delta

- Tags: `CONTRACT`, `OPERATE`, `ROUTE`
- Moved from `projection shape only` to `projection shape settled and next slot
  explicitly assigned to projected-process summary truth`
- Remaining open:
  - implement bounded projected-process summary facts
