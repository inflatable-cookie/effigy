---
title: No-match benchmark isolation 1098 worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: Effigy orchestrator
created: 2026-09-01
updated: 2026-09-01
handoff_path: /Users/tom/Dev/projects/effigy/docs/handoffs/20260901-144054-no-match-benchmark-1098.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, papercuts]
---

## What This Thread Was Doing

PR 71 closed card `1097`. Card `1098` is the next bounded ready Effigy
papercut: the live no-match benchmark stays green only while its query terms
remain absent from the repository documentation that explains it.

This dispatches one bounded implementation lane. No transcript or second
prompt is part of the authority chain.

## Why It Matters

A benchmark should turn red for retrieval regressions, not because a maintainer
documented the benchmark. Fixture-only empty-result proof removes that prose
coupling without changing documentation-context behavior.

## Current State

- **Repository:** `/Users/tom/Dev/projects/effigy`
- **Planning branch:** `main`
- **Planning base commit:** `983f86048daa884a8c8440fbc7b3b8166f818218`
- **Pushed main verification:** the commit containing this handoff must be
  pushed and local `HEAD == origin/main` before launch
- **Planning checkout:** clean before this planning batch
- **Worker mode:** implementation worker dispatched by the orchestrator; this
  handoff activates the worker-only worktree preflight
- **Planning artifacts included at the base:** roadmap `g08.043`, card `1098`,
  and active Next Task pointers
- **Worker branch:** `worker/g08-043-no-match-benchmark-1098`
- **Worker worktree:** launcher-managed
- **Worktree creation command:** Paseo-managed `branch-off` from `origin/main`
- **Worker worktree policy:** follow `Completion Protocol`; launcher worktree
  first, named/manual fallback only when required
- **Required sibling worktree links:** none
- **Active spec lane:** none; the settled benchmark-isolation rule needs no
  strict spec
- **Roadmap milestone:** [`g08.043`](../roadmaps/g08/043-docs-context-no-match-benchmark-isolation-papercut.md)
- **Ready cards, in order:** [`1098`](../roadmaps/g08/batch-cards/1098-isolate-no-match-benchmark-from-live-corpus.md)
- **Allowed runway:** card `1098` only
- **Remaining card budget:** one card
- **Dispatch topology:** one ordinary bounded implementation lane
- **Parallel safety check:** no other Effigy lane owns the benchmark matrix or
  this card's closeout surfaces
- **Surfaces this lane owns:** `scripts/benchmark-docs-context.rhai`, focused
  benchmark proof, the selected `PAPERCUTS.md` entry, roadmap `g08.043`, card
  `1098`, one evidence log, and active Next Task/front-door closeout
- **Integration ownership:** none; this worker owns its bounded closeout
- **Merge ordering:** same-repository PRs merge one at a time; the orchestrator
  refreshes this head against current `main` and re-reviews it if a sibling lane
  merges first
- **Canonical refs:** documentation graph architecture `024`; contracts `001`
  and `041`
- **Review oracle:** card `1098` `## Review Oracle`
- **Model capability profile:** day-to-day worker; bounded Rhai benchmark and
  documentation closeout with settled behavior
- **Frontier-worker justification:** none
- **Tool/runtime restrictions:** do not write the live no-match query into any
  live profile root as temporary proof; do not edit `.github/workflows/` or run
  release mutations
- **Required validation:** card `1098`
- **PR base/head:** current pushed `main` /
  `worker/g08-043-no-match-benchmark-1098`
- **PR URL:** pending
- **Review state:** awaiting implementation
- **Merge path:** orchestrator after accepted review of the current head and
  passing required checks

## Boundaries

- **In scope:** reproduce, repair, prove, and close card `1098`.
- **Out of scope:** retrieval runtime, ranking, weighting, traversal, budgeting,
  profiles, graph storage, refresh/timeout, CLI/JSON, product corpus exclusions,
  catalog-pack publication, release/workflow, S3, and rollover.
- **Outcome shape:** complete issue fix. Own reproduction through the smallest
  contract-valid repair, cleanup, validation, evidence, and PR creation.
- Do not invent architecture, change contracts, widen the roadmap, or choose an
  unresolved product decision.
- Write only inside the named surfaces. Stop on hidden overlap or dependency.
- Work only in the clean worker worktree selected by `Completion Protocol`.
- Do not merge the PR. Merge belongs to the orchestrator.

## Important Context

- **Planning lineage:** card `1090` created both a fixture no-match proof and a
  live-corpus no-match case; the live case broke when its terms were documented.
- **Why this card is ready:** fixture-only ownership, a structural recurrence
  guard, unchanged live authority cases, validation, and stop conditions are
  settled.
- **Decisions and preferences:** keep one non-vacuous fixture-owned empty proof;
  reject live-target empty cases before execution; preserve historical logs as
  historical evidence.
- **Open tensions:** none inside the card. A product exclusion mechanism or any
  retrieval behavior change is a stop condition.
- **Report after:** coherent implementation/proof batch and pushed PR closeout.
- **Report to:** the active Paseo control plane for orchestrator review.

## Suggested Next Move

Run the `Completion Protocol` preflight before broad reads. Then read
`AGENTS.md`, contracts `001` and `041`, roadmap `g08.043`, card `1098`, the
selected papercut, and `scripts/benchmark-docs-context.rhai`.

## Completion Protocol

### Before you start

1. This handoff's worker metadata activates worker mode. Before broad reads,
   run `git rev-parse --show-toplevel`, `git branch --show-current`,
   `git status --porcelain`, and `git worktree list --porcelain`.
2. Accept a clean registered non-`main` launcher worktree. Record its actual
   root/branch and do not create another because its generated names differ.
3. If the launcher context is unusable, follow the repository's configured
   manual fallback. Never guess `/tmp`, clean a dirty checkout, or create a
   second worktree behind the launcher.
4. Run
   `GIT_SSH_COMMAND="ssh -o ConnectTimeout=10 -o BatchMode=yes" git fetch origin`.
   Confirm `HEAD == origin/main`, confirm planning base
   `983f86048daa884a8c8440fbc7b3b8166f818218` is an ancestor, and load the
   tracked handoff from `HEAD`. Stop if it differs from the absolute file.
5. Required sibling links: none.
6. Read the named authority surfaces and run cheap orientation checks.

### While you work

- Execute card `1098` as one coherent issue-fix lane.
- Preserve the remaining live benchmark cases exactly unless the card requires
  their structural relocation.
- Stop on any card stop condition or planning expansion.
- Report meaningful chunks through Paseo.

### When the assigned runway is complete

1. Run card validation and map every oracle row to exact proof.
2. Close card, roadmap, selected papercut, evidence log, and active Next Task
   pointers back to official publication planning; do not open that lane.
3. Reconcile current matrix history/counts without rewriting historical logs.
4. Push the worker branch and open one PR against current pushed `main`.
5. Report PR URL, exact head, validation, unresolved items, and docs QA
   classification. Do not merge.

### Review and merge path

The orchestrator reviews the exact PR head and records its verdict on GitHub.
Requested changes return to this worker. Accepted exact head plus passing checks
and mergeability authorizes orchestrator merge without another prompt.

- **Closeout refs:** card `1098`, roadmap `g08.043`, one dated log,
  `PAPERCUTS.md`, and active front doors

### Handoff closeout

Leave the runway honest. If blocked, record the blocker and stop rather than
marking the card complete.
