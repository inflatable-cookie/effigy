---
title: Docs context latency and freshness worker handoff
kind: northstar-handoff
handoff_mode: worker-pr-loop
worker_mode: implementation
dispatch_authority: orchestrator
handoff: single-file-path-only
status: ready-to-launch
owner: graph freshness, lazy refresh, search-index rebuild, docs-context query, graph time-budget diagnostics
created: 2026-09-05
updated: 2026-09-05
handoff_path: /Users/tom/Dev/projects/effigy/docs/handoffs/20260905-105800-docs-context-latency-1113.md
base_required: pushed-main
tags: [coordination, handoff, worker, pr, g09.005, 1113, frontier]
---

## What This Thread Was Doing

The coordinator is dispatching the single ready serial lane for docs-context
latency and freshness. Card `1113` owns controlled reproduction, root-cause
measurement, the smallest bounded repair, evidence, validation, and a PR.

This dispatches one bounded implementation lane. No transcript or second prompt
is part of the authority chain.

## Why It Matters

`effigy docs context` must answer within the frozen agent-sized budgets on a
current index and complete a small stale incremental refresh without changing
contract `041` semantics or inventing a second indexing system.

## Current State

- **Repository:** `effigy` at `/Users/tom/Dev/projects/effigy`
- **Planning branch:** `main`
- **Planning base commit:** `7f75092d669c3281a674fdf2a3f6097b67d3b523`
- **Pushed main verification:** clean `main`, `HEAD == origin/main`
- **Planning checkout:** clean
- **Worker mode:** implementation worker dispatched by the orchestrator; this handoff activates the worker-only worktree preflight.
- **Planning artifacts included at the base:** completed `g09.004` / card `1112` / spec `119`; ready `g09.005` / card `1113` / spec `120`
- **Worker branch:** `worker/g09-005-docs-context-latency-1113`
- **Worker worktree:** `/Users/tom/.paseo/worktrees/310mya31/g09-005-docs-context-latency-1113`
- **Worktree creation command:** Paseo `create_workspace`; `isolation: worktree`, `mode: branch-off`, `baseBranch: origin/main`
- **Worker worktree policy:** follow `Completion Protocol`; launcher worktree first, named/manual fallback only when required.
- **Required sibling worktree links:** `none`
- **Active spec lane:** [`docs/specs/120-docs-context-latency-and-freshness-strict-lane.md`](../specs/120-docs-context-latency-and-freshness-strict-lane.md)
- **Roadmap milestone:** [`docs/roadmaps/g09/005-docs-context-latency-and-freshness.md`](../roadmaps/g09/005-docs-context-latency-and-freshness.md)
- **Ready cards, in order:** [`docs/roadmaps/g09/batch-cards/1113-docs-context-latency-and-freshness.md`](../roadmaps/g09/batch-cards/1113-docs-context-latency-and-freshness.md)
- **Allowed runway:** execute card `1113` steps 1 through 5 only
- **Remaining card budget:** one card, one PR
- **Coordinator agent ID:** `0accca7b-4f0e-428a-b62c-b8755b32cc1c`
- **Delivery route:** coordinator-attached child with `notifyOnFinish: true`; the coordinator records scoped creation and returned child/workspace identity.
- **Dispatch topology:** sole ready-frontier lane after serial prerequisite `1112`
- **Parallel safety check:** no approved siblings; serial edge satisfied by merge `f1732c87`; no shared mutable scope
- **Surfaces this lane owns:** `crates/effigy-codegraph/src/**`, `src/runner/graph_time_budget.rs`, `src/runner/docs_command/**`, `src/runner/graph_command/**` where present, `scripts/benchmark-docs-context.rhai` freeze-history append only, tests under those crates and `src/tests/**`, guides `079` and `076`
- **Integration ownership:** coordinator owns `CHANGELOG.md` `[Unreleased]`, `docs/logs/2026-09/`, `docs/logs/README.md`, this roadmap, card `1113`, spec `120`, contract `041` Next Task unless drift fires, and planning front doors
- **Merge ordering:** same-repository PRs merge one at a time; the orchestrator refreshes this head against current `main` and re-reviews it if a sibling lane merges first
- **Canonical refs:** architecture `docs/architecture/024-repository-defined-documentation-graph.md`; contract `docs/contracts/041-documentation-graph-profile-contract.md`; guides `docs/guides/079-documentation-graph-profiles-and-context.md`, `docs/guides/076-code-graph-and-agent-workflows.md`
- **Review oracle:** card `1113` Review Oracle and spec `120` Whole-Lane Review Oracle
- **Model capability profile:** frontier-capable implementation worker; measurement discipline and graph-crate root-cause work are required
- **Worker provider/model identity:** `claude/claude-opus-5`
- **Frontier-worker justification:** exceptional reasoning — frozen multi-condition Effigy/fixture measurements must attribute warm, stale-incremental, and cold costs before repair, with no concurrent graph process; material consequence — a wrong repair can alter shared graph freshness, timeout, provenance, and agent retrieval behavior across repositories, while the lane is a gate for planned cross-repository source routing
- **Tool/runtime restrictions:** no second index, daemon, embeddings, engine rewrite, new flag, default-budget change, contract `041` semantic change, schema ID bump, workflow edit, or edits outside Effigy and the named fixture; no concurrent graph process during measurements
- **Required validation:** controlled before/after measurements; `effigy perf:docs-context-benchmark`; focused codegraph tests; `effigy qa`; `cargo fmt --all -- --check`; `cargo clippy --all-targets -- -D warnings`; `git diff --check`
- **PR base/head:** current pushed `main` at `7f75092d`; head pending
- **PR URL:** pending
- **Review state:** awaiting implementation and PR
- **Merge path:** orchestrator after accepted independent exact-head review and passing required checks

## Boundaries

- **In scope:** reproduce cold, stale-incremental, and warm behavior with all spec `120` identity fields and no concurrent graph process; meet frozen budgets; repair only the existing refresh path, lock, health snapshot, and typed timeout; add timeout-detail fields only; replay the unchanged fixture benchmark and K4/K5/no-match questions; update guides and evidence.
- **Out of scope:** second index, daemon, embeddings, new flag, default-budget change, contract `041` semantic changes, workflow edits, broad engine rewrite, cross-repository routing, or product planning.
- **Outcome shape:** smallest evidence-led contract-valid repair with before/after measurements, validation, evidence, and a reviewable PR. Do not stop at diagnosis-only unless a named stop condition prevents safe implementation.
- Do not invent architecture, change contracts, widen the roadmap, or choose unresolved retrieval semantics.
- Write only inside **Surfaces this lane owns**. Leave closeout/front-door surfaces assigned to **Integration ownership** to the coordinator.
- Work only in the clean worker worktree selected by `Completion Protocol`; do not merge.

## Important Context

- **Planning lineage:** Northstar shared-knowledge retrieval pilot -> operator-confirmed direction -> `g09.005` / spec `120` / card `1113`, unblocked by merged `g09.004` at `f1732c87`.
- **Why this card is ready:** the manifest freezes the identity fields, budgets, reproduction matrix, allowed repair seams, review oracle, and stop conditions; the serial prerequisite is complete.
- **Decisions and preferences:** measure before changing; use release-profile binaries; warm Effigy success under `EFFIGY_GRAPH_TIMEOUT_MS=5000` with p50 target ≤2000 ms; stale incremental ≤50 Markdown files under default budget target ≤30 s then warm 5000 ms success; cold index must not regress >10%; fixture benchmark must pass warm at 5000 ms.
- **Open tensions:** any budget miss requiring contract `041` semantic change, default-budget change, or a new indexing architecture returns to planning through Chatterbox.
- **Report after:** the reproduced before-table, phase attribution, bounded repair, replay, and final validation/PR.
- **Report to:** the owning coordinator through the linked child result. Do not message Chatterbox during automatic dispatch.

## Suggested Next Move

Run the `Completion Protocol` preflight before broad reads. Then read the active
milestone, card, `AGENTS.md`, architecture, contract, and guides from the
selected worktree. Confirm no other graph process is running before the first
measurement. Start with the controlled before-table.

## Completion Protocol

Use the standard worker completion protocol in the Northstar orchestrator
handoff template. Verify the tracked handoff at the exact pushed base; execute
only card `1113`; preserve the benchmark matrix; falsify every review-oracle
counterexample; keep reserved closeout surfaces for the coordinator; push the
branch; and open a reviewable PR without merging it.

The orchestrator will launch an independent reviewer in this same worker
workspace under a serial clean exact-head lease. The reviewer must use a
provider/model identity distinct from `claude/claude-opus-5`, post a durable
verdict naming the exact head SHA, and make no tracked changes.

- **Closeout refs:** card `1113`, roadmap `g09.005`, spec `120`, one dated evidence log under `docs/logs/2026-09/`, `CHANGELOG.md`, and the named front doors

Before calling the runway complete, leave the card, roadmap, log, and next-task
state honest. The coordinator reconciles and closes those surfaces after merge.
