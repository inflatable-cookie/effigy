[package]
name = "effigy-rhai"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Rhai runtime and host API contracts for Effigy"

[dependencies]
effigy-core = { path = "../effigy-core" }
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
rhai = { version = "1", features = ["serde"] }
serde_json = "1"
signal-hook = "0.3"
toml = "0.8"
