[package]
name = "effigy-process"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Process supervision subsystem for Effigy (spawn, stdio streaming, shutdown, diagnostics)"

[dependencies]
nix = { version = "0.31", default-features = false, features = ["process", "signal"] }
