services:
  {{ service_name }}:
    image: postgres:{{ version }}-alpine
    environment:
      POSTGRES_DB: "{{ database }}"
      POSTGRES_PASSWORD: "{{ password }}"
    ports:
      - "5432:5432"
    volumes:
      - {{ project_name }}-{{ service_name }}-data:/var/lib/postgresql/data
