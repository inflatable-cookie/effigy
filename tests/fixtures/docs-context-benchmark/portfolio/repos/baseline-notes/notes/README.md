# Baseline notes

## Tolerance ledger intake

The tolerance ledger intake desk records every band adjustment before the
custodian sees it. This repository declares no documentation graph profile, so
these sections are scoped by baseline Markdown rules.
