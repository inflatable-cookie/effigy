# Application port @type=port @required
PORT=3000

# Runtime mode @type=enum(development,staging,production)
NODE_ENV=development

# Hostname resolved from shell
HOSTNAME=exec('printf api')

# API base URL @type=url
API_BASE=https://${HOSTNAME}.example.test:${PORT}/v1

# Session secret @sensitive @required
SESSION_SECRET=
